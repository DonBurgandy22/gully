"""
Burgandy Cognitive Framework — src package.

Modules:
    models          — Pydantic data models (Node, Edge, Cluster, SimulationResult)
    graph_builder   — JSON data loading + networkx DiGraph construction
    activation_engine — Activation propagation with decay, cap, and loop protection
    loop_engine     — Cycle detection, classification, and loop utilities
    scoring         — Centrality, influence score, and path cost analysis
    cluster_manager — Domain cluster attachment and extensibility helpers
    simulation      — Demo 1, 2, 3 simulation entry points
    visualization   — pyvis HTML and matplotlib PNG generation
    utils           — File I/O, path resolution, and report writing
"""
