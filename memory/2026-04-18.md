## Pre-compaction memory flush

### Cognitive framework patch analysis (visualization.py)
- **Local block**: pending edges handling in `generate_3d_threejs()`
- **Insertion point**: Line 67 (after `edgeMap[key] = {line, pulse, src:src.mesh.position, tgt:tgt.mesh.position, active:false, t:0};`)
- **Missing snippet**: Create missing edges from `state.all_edges` using same pattern as pending edges
- **Status**: Ready to apply
- **Pattern**: Loop over `state.all_edges`, check `edgeMap[key]`, create edge if missing using `nodeMap` lookup and `THREE.Line` + `THREE.Sphere` geometry/material

### Current system reality
- Workspace: `C:\Burgandy`
- Runtime state: `.openclaw`
- Model: `ollama/qwen3.5:4b`
- WhatsApp active: +27602678740 (just reconnected)
- No whole-file rewrites on cognitive framework
- Patch is minimal insertion only, no full refactor
